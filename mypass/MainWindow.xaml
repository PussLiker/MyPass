<Window x:Class="mypass.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mypass"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="MainWindow" Height="680" Width="1080" 
        WindowStartupLocation="CenterScreen" Background="Transparent"
        AllowsTransparency="True"
        >
    <Window.Resources>

        <SolidColorBrush x:Key="Background" Color="#1a1a1d" />
        <SolidColorBrush x:Key="TextColor" Color="#4e4e50" />
        <SolidColorBrush x:Key="DarkPink" Color="#6f2232" />
        <SolidColorBrush x:Key="Pink" Color="#950740" />
        <SolidColorBrush x:Key="lightPink" Color="#c3073f" />

    </Window.Resources>
    
    <Grid>
        <Border Background="{DynamicResource Background}" CornerRadius="10"/>

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
                
            </Grid.ColumnDefinitions>
            
        </Grid>
        
    </Grid>
</Window>
